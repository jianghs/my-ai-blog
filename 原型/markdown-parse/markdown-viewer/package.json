{
  "name": "markdown-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "parse": "node scripts/parse.js",
    "parse:full": "node scripts/parse.js --full",
    "clean": "node scripts/clean.js",
    "dev": "npm run parse && vite",
    "build": "npm run parse && vite build",
    "build:full": "npm run parse:full && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "gray-matter": "^4.0.3",
    "highlight.js": "^11.11.1",
    "marked": "^15.0.7",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}